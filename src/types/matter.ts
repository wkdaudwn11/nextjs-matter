export type BodyRectangle = {
  angle?: number;
  height: number;
  image?: string;
  width: number;
  x: number;
  y: number;
};

export type BodyCircle = {
  image?: string;
  radius: number;
  x: number;
  y: number;
};
